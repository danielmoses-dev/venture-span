{
  "name": "venturespan",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ml\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:ml": "cd ml-service && venv\\Scripts\\python -m uvicorn main:app --reload --port 8000",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "install:all": "npm install && cd ml-service && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
